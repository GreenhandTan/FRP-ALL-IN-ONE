version: '3.8'

services:
  # 1. 核心管理后台 (Python API)
  backend:
    build:
      context: ..
      dockerfile: server/Dockerfile
    container_name: frp-manager-backend
    restart: always
    user: root  # 需要 root 权限创建文件和启动进程
    network_mode: "host" # 与 Nginx 和 FRPS 同一网络，方便通信
    volumes:
      - frp-data:/opt/frp       # FRP 文件持久化
      - ./frps.toml:/app/frps.toml  # FRPS 配置文件（可读写）
      - /var/run/docker.sock:/var/run/docker.sock  # Docker Socket（允许重启容器）

  # 2. Web 界面 (Nginx + React)
  frontend:
    build: ../frontend
    container_name: frp-manager-web
    restart: always
    network_mode: "host" # 直接监听宿主机 80 端口，不再使用端口映射
    depends_on:
      - backend  # 串行构建，避免内存叠加

  # 3. FRP Server (官方镜像)
  frps:
    image: docker.1ms.run/snowdreamtech/frps:latest
    container_name: frps
    restart: always
    network_mode: "host" # 🔥 只有 host 模式才能高性能且无限制地使用所有宿主机端口
    volumes:
      - ./frps.toml:/etc/frp/frps.toml
    # host 模式下 ports 映射无效，所有端口直接监听在宿主机

# 命名卷，确保重启后数据不丢失
volumes:
  frp-data:
